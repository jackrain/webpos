/*****************************************************
 * 名称：webpos 2.0 接口公布中心
 * 描述：主要将当前模块写好的一些相关接口公布到接口中心里 
 *       其他模块只要使用接口中心即使用已经公布的接口
 ******************************************************/
var path = require('path');
var framework = require('framework');

var APICenter = function () { }

//公布接口中心主体实例
module.exports = new APICenter;

/*
 * 名称：动态加载当前模块指定api接口
 * 参数(name)：接口路径名称
 */
APICenter.prototype.require = function (name) {
    name = framework.String.EndsWidth(".api.js", true)?name:framework.String.Format("{0}.api.js", name);
    var file = path.join(__dirname, name);
    return require(file);
}

/*
 *名称：会员接口 
 *描述：包含会员增删改等小接口
 */
APICenter.prototype.VIPAPI = require('./common/demo.api.js');