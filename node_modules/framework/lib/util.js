var Utils = function () { }
var strUtil = require('./string.js');

module.exports = new Utils();

//将传入值v如果在为null或者空字符串或者空格字符串 情况下 替换值为dv
Utils.prototype.Undef = function (v, dv) {
    if (strUtil.IsNullOrWhiteSpace(v)) {
        return dv;
    } else {
        return v;
    }
}