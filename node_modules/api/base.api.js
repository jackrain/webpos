/*****************************************************
 * 名称：webpos 基础接口定义
 * 描述：可以使用此接口派生出相关子接口 同时为子接口提供基础接口函数
 * 作者：Beven
 ******************************************************/
var BaseAPI = function () { }

//公布基础接口实例
module.exports = new BaseAPI();
var baseapi = new BaseAPI();

//框架组件
BaseAPI.prototype.Framework = require('framework');
//数据库访问组件
BaseAPI.prototype.Accessorlib = require('Accessor');

/*
 * 名称：创建一个继承与当前接口的子接口类
 * 参数(publish)：是否发布立即发布接口
 *              值不为null时 
 *              publish.exports = new api;公布成当前派生接口的实例
 *              否则 需要手动公布当前接口
 */
BaseAPI.prototype.createAPI = function (publish) {
    var drive = function () { };
    drive.prototype = baseapi;
    drive.prototype.__base = new BaseAPI();
    if (publish != null) {
        publish.exports = new drive();
    }
    return drive;
}